target 'MOBReview_iOS' do
    use_frameworks!
    platform :ios, '8.0'
    podspec :path => 'MOBReview.podspec'
end

target 'MOBReview_WatchOS' do
    use_frameworks!
    platform :watchos, '2.0'
    podspec :path => 'MOBReview.podspec'
end
